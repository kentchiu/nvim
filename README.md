# nvim
nvim config


# Installation

Symlink ~/.config/nvim to this project.

```shell
$ mkdir -p ~.config
$ cd ~/.config
$ ln -s ~/dev/kent/nvim/nvim
$ nvim ~/.config/nvim/init.vim

```

And run: `:PlugInstall`.
