# Kent's Neovim Config

## Check which markdown plugin is using.

```bash
# open lazy vim info panel
:lazy

# open a makrdown file and check filetype (should be echo markdown)
:echo &filetype

# list autocmd of markdown
:autocmd FileType markdown
```
